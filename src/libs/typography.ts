import { StyleSheet } from "react-native";
import { THEME } from "./theme";

export const TYPOGRAPHY = StyleSheet.create({
  heading: {},
  subheading: {},
  body: {
    fontWeight: "bold",
  },
  numbers: {
    fontSize: 14,
    fontWeight: "bold",
  },
});
